# Legado da VPS — não executar

Todo o conteúdo deste diretório pertence à arquitetura aposentada baseada em VPS, Docker e Caddy.

Estado vigente:

- portal público hospedado no GitHub Pages;
- VPS `76.13.226.21` aposentada;
- nenhum script deste diretório integra o pipeline atual;
- nenhuma instrução aqui deve ser usada para deploy, rollback ou alteração de produção.

Os arquivos permanecem apenas para auditoria histórica.

Fonte vigente:

- `.github/workflows/pages.yml`
- `.github/workflows/ci.yml`
- `tests/validate_static.py`
- `docs/VPS_APOSENTADA.md`
